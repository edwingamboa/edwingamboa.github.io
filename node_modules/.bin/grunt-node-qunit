#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-node-qunit/bin/grunt-node-qunit" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-node-qunit/bin/grunt-node-qunit" "$@"
  ret=$?
fi
exit $ret
